# Claude Code Prompt — Issue #60 Fix + CP-020 SOURCES Check
# Date: 2026-06-03 | Prepared by: CoWork

---

## Task 1 — CP-020 SOURCES appendix check

```bash
cat /root/openclaw_phase5/data/final_output_china_monitor_001.txt | grep -A 200 "SOURCES" | head -80
```

Look for source_category (CN-STATE, INTL-WIRE, CN-BUSINESS, etc.) and freshness_label (SAME-DAY, YESTERDAY, etc.) on each source entry. Report first 3–5 entries verbatim.

---

## Task 2 — Issue #60: query_builder.py fix

### Step 1 — Find and read query_builder.py

```bash
find /root/openclaw_phase5 /root/openclaw_phase7 -name "query_builder.py" 2>/dev/null
cat [result path]
```

### Step 2 — Check what's exported from loader.env in run_light_to_lark.sh

```bash
grep -n "QUERY_TEMPLATE_SET\|query_template_set\|loader.env\|export OPENCLAW" /root/run_light_to_lark.sh | head -30
cat /root/openclaw_runtime/china_monitor_001/loader.env 2>/dev/null || echo "no loader.env"
```

### Step 3 — Read load_client_config.py

```bash
find /root -name "load_client_config.py" 2>/dev/null
cat [result path]
```

### Step 4 — Implement the fix

Backup first:

```bash
cp [query_builder.py path] [query_builder.py path].bak_20260603_issue60
```

The fix must:

1. Read `OPENCLAW_CLIENT_ID` from env (already exported by run_light_to_lark.sh)
2. Determine `query_template_set` — either read `OPENCLAW_QUERY_TEMPLATE_SET` if exported, or read from `/root/openclaw_runtime/{CLIENT_ID}/loader.env` directly
3. Select queries based on `query_template_set`:

**`china_monitor_v1`** → existing WS1 queries, no behaviour change

**`alj_china_auto_weekly_v1`** → 7 Baidu queries (OPENCLAW-RQT-002 v1.1, approved 2026-06-02):

| query_id | topic_group | baidu_string |
|----------|-------------|--------------|
| auto_market_p1 | market_pulse | 中国 汽车 销量 新能源汽车 本周 乘联会 中汽协 |
| oem_watch_p1 | oem_watch | 丰田 比亚迪 吉利 长安 汽车市场 销量 市场份额 本周 |
| oem_watch_c1 | oem_watch | 丰田 中国 战略 中国车企 海外扩张 中东 过去一个月 |
| export_gulf_p1 | export_gulf | 中国 新能源汽车 出口 沙特阿拉伯 海湾 车企 本周 |
| export_gulf_c1 | export_gulf | 中国 汽车 出口 海湾 车企 海外布局 中东 过去一个月 |
| dealer_economics_p1 | dealer_economics | 中国 汽车 经销商 库存 降价 售后 经营 经销商协会 本周 |
| policy_p1 | policy | 工信部 新能源汽车 政策 补贴 乘用车 汽车行业 本周 |

**Unknown template_set** → raise `ValueError` with clear message (do NOT silently fall back to WS1)

4. Return queries in the same dict/list structure WS1 uses (match existing schema exactly)

### Step 5 — If OPENCLAW_QUERY_TEMPLATE_SET is NOT already exported

Add export following the same pattern as other loader vars (e.g. OPENCLAW_BRAIN_CONTEXT). Show diff before applying, then:

```bash
bash -n /root/run_light_to_lark.sh && echo "bash syntax ok"
```

### Step 6 — Validate

```bash
python3 -m py_compile [query_builder.py path] && echo "py_compile ok"
```

### Step 7 — Report back

1. CP-020 SOURCES first 3 entries verbatim
2. query_builder.py path and original structure (how queries were hardcoded)
3. Whether QUERY_TEMPLATE_SET needed to be added to run_light_to_lark.sh
4. Backup path confirmed
5. py_compile and bash -n results
6. Any unexpected findings — stop and report before proceeding if structure differs from above

---

## Context

- WS1: `china_monitor_001`, template `china_monitor_v1`
- WS2: `alj_china_auto_001`, template `alj_china_auto_weekly_v1`
- Issue #60: all ALJ pilot runs used WS1 queries; governance step 7 must re-run after fix
- ALJ is `pilot_mode=true` — safe to test immediately after fix
- Do NOT modify validator, scrubber, agent prompt, or delivery gate
